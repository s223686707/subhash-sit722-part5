apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-management-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: inventory-management
  template:
    metadata:
      labels:
        app: inventory-management
    spec:
      containers:
      - name: inventory-management
        image: $CONTAINER_REGISTRY/inventory_management:$VERSION
        ports:
        - containerPort: 9000
        env:
        - name: DATABASE_URL
          value: "postgresql://db_y63t_user:r8j9Be5AngN5dO1BtVVBdJQ3ULM6u82O@dpg-crpsafjtq21c73d7bkmg-a.oregon-postgres.render.com/db_y63t"
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-management-service
spec:
  type: LoadBalancer
  selector:
    app: inventory-management
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000